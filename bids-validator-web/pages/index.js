// A placeholder component to trigger an actual index.html file when using `next export`.
// This component won't be used since _app.js renders its own component, without any children.

export default () => null;
